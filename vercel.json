{
  "version": 2,
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "50mb",
        "handler": "api/index.handler"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/webhook"
    }
  ]
}
