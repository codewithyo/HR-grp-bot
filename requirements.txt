# Telegram MTProto client (used for API calls — ban, mute, send_message, etc.)
pyrogram==2.0.106

# Required for Pyrogram's crypto operations
tgcrypto

# Web framework — receives webhook updates from Telegram
fastapi

# HTTP client — used in /api/setup_webhook to call Telegram Bot API
httpx

# ASGI server for local runs and container startup
uvicorn[standard]
